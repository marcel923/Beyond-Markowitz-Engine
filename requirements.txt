dash>=3.3,<4.0  # UWAGA: Dash 4.x przepisał dcc.Slider/dcc.Dropdown od zera (nie używają już rc-slider/react-select),
                # przez co cały custom dark-theme CSS w app.index_string przestaje trafiać w jakiekolwiek elementy.
                # Zostajemy na gałęzi 3.x do czasu migracji stylowania na nowe API Dash 4.
yfinance>=0.2.60
plotly==5.24.1
pandas>=2.2.3
numpy>=1.26
scipy>=1.13
statsmodels>=0.14  # engine/pairs.py -- Engle-Granger cointegration test (statsmodels.tsa.stattools.coint),
                    # NOT a plain adfuller() on regression residuals (see engine/pairs.py module docstring
                    # for why that distinction matters -- plain ADF critical values understate how
                    # extreme the test statistic needs to be for residuals from an estimated regression).
scikit-learn>=1.9
networkx>=3.2  # engine/pairs.py -- max_weight_matching (exact Blossom algorithm) for the pair-assignment
                # problem: every company can join AT MOST one pair, chosen to maximize total t-statistic
                # across the whole universe -- NOT a greedy "take the best pair, then the next" heuristic,
                # which can miss a strictly better combination (see PROJECT_CONTEXT.md for the worked
                # counter-example that motivated using the exact algorithm instead).
diskcache>=5.6  # ui/app_instance.py -- backs Dash's DiskcacheManager for background_callback_manager,
                # letting long-running theta-mechanism callbacks (Tabs 4/5/6, pair-matching panel)
                # report live text progress instead of a spinner with zero feedback for 10+ minutes.
multiprocess>=0.70  # required by dash's DiskcacheManager itself (pip install "dash[diskcache]" pulls
                     # this in too, but pinning it explicitly here avoids relying on that extras syntax)
SALib>=1.4.7  # engine/sobol_analysis.py -- Sobol/Morris global sensitivity analysis and Saltelli sampling